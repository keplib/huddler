import {Category} from '../../types'
 
type Props = {
  currentUserInterests: Category[];
}

const UpdateInterests = ({currentUserInterests}: Props) => {
  return <h1> Under construction, waiting for the categories component</h1>
}

export default UpdateInterests